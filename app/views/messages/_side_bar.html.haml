.side-bar
  .side-bar__header
    .side-bar__header__name
      .side-bar__header__name__left
        = current_user.name
      .side-bar__header__name__right
        = link_to new_group_path do
          %i.fa.fa-edit
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog', class: 'icon')
  .side-bar__group-list
    - current_user.groups.each do |group|
      .side-bar__group-list__box
        = link_to '#', class: "side-bar__group-list__box__name" do
          = group.name
          .side-bar__group-list__box__message
            メッセージはまだありません